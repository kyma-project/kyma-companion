id: test-question-21 # format: test-<resource_kind>-<test_id>
description: Unrelated to existing cluster resources, ask how to get started with Kyma.
queries:
  - user_query: How to get started with kyma?
    resource:
      kind: Cluster
      api_version: ""
      name: ""
      namespace: "" # format: test-<resource_kind>-<test_id>
    expectations:
      - name: aquire_k8s_cluster
        statement: points out how to aquire a k8s (kubernetes) cluster
        threshold: 0.5
        required: true
      - name: mentions_btp
        statement: mentions btp (Business Technology Platform)
        threshold: 0.5
        required: true
      - name: mentions_modules
        statement: mentions the Kyma modules
        threshold: 0.5
        required: true
      - name: provides_links
        statement: provides links to further topics
        threshold: 0.5
        required: false

