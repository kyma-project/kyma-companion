id: test-question-27 # format: test-<resource_kind>-<test_id>
description: Unrelated to existing cluster resources, ask for kyma usecases.
queries:
  - user_query: What can I use Kyma for?
    resource:
      kind: Cluster
      api_version: ""
      name: ""
      namespace: "" # format: test-<resource_kind>-<test_id>
    expectations:
      - name: mentions_sap
        statement: mentions that kyma can be used with SAP services.
        threshold: 0.5
        required: true
      - name: mentions_BTP
        statement: mentions the BTP (Business Technology Platform)
        threshold: 0.5
        required: true
      - name: provides_links
        statement: provides links to further topics
        threshold: 0.5
        required: false


